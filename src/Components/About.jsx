import React from 'react';

const About = () => {
  return (
    <div className="container mt-5">
      <h2>About This Movie App</h2>
      <p>This React app shows movie data using the OMDB API. It uses React Router, Axios, Bootstrap, and reusable components.</p>
    </div>
  );
};

export default About;
